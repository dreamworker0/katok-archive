# 파이썬 의존성은 이것 하나다.
#
# 파이프라인의 나머지(파싱·증분 반영·발행본 생성·검사)는 표준 라이브러리로 돈다.
# 의존성이 적으면 매일 밤 무인으로 도는 자동화가 그만큼 덜 깨진다 — 갱신이 멈추는
# 이유가 하나 줄어드는 값은 편의 라이브러리 몇 개보다 크다.
#
# Pillow 를 쓰는 곳은 사진을 다루는 세 군데뿐이다:
#   scripts/build_thumbnails.py   갤러리용 작은 사진
#   scripts/build_pwa_icons.py    앱 아이콘
#   scripts/optimize_ui_art.py    화면 삽화 최적화
#
# 동영상 미리보기는 Pillow 가 mp4 를 못 읽어 `ffmpeg` 을 부른다. PATH 에 없으면
# 그 단계만 건너뛰고 갱신은 계속된다 — 미리보기 하나 때문에 그날 발행을 멈추지 않는다.
pillow>=11,<13
