# 보고서 규칙

주제 보고서(`output/reports/t-XXX.md`)를 어떻게 쓰는가. **사람이 손으로 쓸 때도,
밤 자동 갱신이 쓸 때도 같은 규칙이다.**

## 규칙 원본은 코드에 있다

규칙 문장 자체는 `scripts/topic_reports.py` 의 `REPORT_RULES` 하나뿐이고, 보고서를
쓰는 두 프롬프트가 **그것을 그대로 읽는다**.

| 쓰는 곳 | 언제 | 프롬프트 |
|---|---|---|
| 분류하면서 함께 | 매일 밤 23:40 새 대화 | `classify_unsorted.build_prompt` |
| 보고서만 다시 | 사람이 불러서 (`--rewrite-*`) | `classify_unsorted.build_report_prompt` |

이 문서에 규칙을 옮겨 적지 않는 이유: **예전에 규칙이 두 곳에 따로 적혀 있어서
사고가 났다.** 분류 프롬프트에는 "링크나 사진 자리표를 넣지 마세요. 자료는 화면
아래에 따로 붙습니다" 만 있었고 인용·절 규칙은 다른 프롬프트에만 있었다. 그래서
2026-07-27 밤 자동 갱신이 만든 보고서는 한 덩어리 산문이고 사진·링크가 전부 글 끝에
모였다. 314편 중 32편이 그 상태였다. 규칙은 한 군데만 있어야 한다.

## 지켜야 하는 두 가지

`scripts/topic_reports.py` 의 숫자와 검사가 같은 값을 본다.

- **인용** — 대화 `QUOTE_REQUIRED_FROM`(6)건 이상이면 짧은 인용(`>`)이 하나는 있어야
  한다. 한 인용은 `MAX_VERBATIM_CHARS`(40)자를 넘기지 않는다.
- **절 나눔** — 대화 `SECTION_REQUIRED_FROM`(10)건 이상이면 `## 짧은 제목` 으로
  나눈다.

### 인용이 왜 규칙인가 — 사진·링크의 자리를 정하는 일이다

사진·링크·첨부는 `place_context_anchors` 가 **인용을 기준으로** 본문 사이에 끼운다.
인용이 없으면 끼울 자리가 없어 **자료가 전부 글 끝으로 밀린다.** '글 따로 자료 따로'가
되는 것이고, 읽기 불편하다는 지적이 정확히 이것이었다.

그래서 자료가 오간 대목을 인용으로 짚는 것이 곧 그 자료의 자리를 정하는 일이다.
`![[...]]` 자리표를 손으로 넣지는 않는다 — 없는 자료를 가리켜 화면이 깨진다.

## 태그는 지어내지 않고 고른다

태그 규칙도 원본이 하나다 — `topic_reports.tag_rules(어휘)`. 두 프롬프트가 그것을
읽고, 목록에 있는 말을 먼저 고르게 한다. 목록에 없는 말은 **한 편에 하나까지만**
새로 만든다(처음 나온 도구·앱 이름 같은 것).

왜 이 규칙이 생겼나: 태그 1,224종 중 1,090종이 **딱 한 번만 쓰였다.** 표기 차이가
아니라 보고서마다 태그를 새로 지어낸 것이다. 표기 통일·넓은 태그 승격 같은 사후
봉합으로는 1회짜리의 약 10%만 구제된다는 것을 실측했다(2026-07-29). 나머지는 애초에
다른 말로 지어져 기계가 합칠 근거가 없다. 그래서 만들 때 고르게 한다.

어휘는 `tags.vocabulary()` 가 매번 새로 만든다 — **두 번 이상 쓰인 태그**만
많이 쓰인 순으로(최대 180개). 한 번 쓰인 말까지 보여주면 그 목록이 곧 지어낸 말들의
목록이 된다. 사람 이름과 `config/tag_places.json` 에 적은 지명·기관 이름은 빠진다.

여기에 `config/tag_broader.json` 의 **넓은 태그**가 횟수와 무관하게 붙는다(잘라내지도
않는다). 사람이 '이것이 이 방의 공통 어휘다' 라고 적어 둔 말이라 고르게 하려고 넣는
것이다 — 'AI 모델'·'클라우드'·'구글 워크스페이스' 처럼 여러 도구를 덮는 말이 그렇다.

기존 보고서는 손대지 않는다. 앞으로 쓰는 것부터 적용된다.

### 넓은 태그 표는 사후에도 쓰인다

같은 표가 발행할 때 **좁은 태그에 넓은 태그를 덧붙인다**(`tags.rollup_parent_tags`).
글자가 겹치는 상하위는 코드가 알아서 붙이지만('온톨로지 모델링' → '온톨로지'),
'앱스스크립트' 가 '구글 워크스페이스' 에 든다는 것은 글자에 단서가 없어 표가 필요하다.

무엇을 적을지는 발행 로그가 알려준다 — 부모도 없고 한 번만 쓰인 태그, 즉 검색
말고는 입구가 없는 태그의 목록이다(실측 2026-08-14 에 843개였다).

```
[태그] 부모도 없고 한 번만 쓰인 태그 782개 — 검색 말고는 입구가 없습니다. …
```

## 어긴 것을 어떻게 찾고 고치나

발행할 때마다 검사해서 알려준다.

```
[주의] 구조 규칙을 어긴 보고서 32개 — 고치려면 …
```

고치는 명령 (한 번에 N편, LLM 값이 든다 — 2026-07-28 실측 편당 약 $0.15):

```bash
python -m scripts.classify_unsorted --rewrite-unstructured 5
```

**매일 돌리지 않는다.** 규칙이 프롬프트에 들어간 뒤로 새 보고서는 규칙을 지키므로,
이 명령은 규칙이 없던 시절의 보고서를 사람이 불러 정리하는 용도다. 새로 쓴 보고서가
규칙을 어기면 그 자리에서 `[규칙 미달: … 없음]` 이라고 로그에 찍힌다.

## 분량은 따로 본다

구조와 분량은 다른 검사다. 분량은 `MIN_BODY_BY_COUNT`(대화 건수별 최소 글자)로 보고
`--rewrite-thin` 으로 고친다. **길게만 쓴 한 덩어리 산문은 분량 검사에 안 걸린다** —
그것을 잡는 것이 위의 구조 검사다.

# AI 보고서 규칙

사람 보고서 옆에 붙는 **기계의 검증 주석**이다(`output/ai-reports/t-XXX.md`).
사람이 쓴 보고서를 두 모델이 따로 검토해 얹는 글이고, 사람 보고서와 **섞지
않는다** — 화면에서도 띠를 두른 별도 블록으로 나온다.

규칙 문장은 여기 옮겨 적지 않는다. **원본은 `scripts/topic_reports.py` 의
`AI_REPORT_RULES` 하나다.** 위에서 적은 것과 같은 이유다 — 규칙이 두 곳에 있으면
갈라진다.

## 통과 기준은 '합의' 가 아니라 '원 출처'

처음 판은 **두 모델이 합의한 것만 싣는다**는 규칙이었다. 2026-08-27 첫 일곱 편을
만들면서 그 규칙이 두 번 실패했다.

**하나. 합의가 오류를 통과시켰다.** 두 모델이 파이어베이스 Hosting 무료 전송량을
"Blaze 는 월 10GB" 로 사이좋게 동의했다. 공식 요금 페이지를 열어 보니 두 요금제
모두 하루 360MB 였다. 동의는 오류를 막은 것이 아니라 **보증서를 붙여 준 셈**이었다.

**둘. 몰아붙여 얻은 동의가 맞는 답을 지웠다.** 한 모델이 '한국디지털사회복지학회'
가 실재한다고 옳게 답했는데, 다른 쪽 검색이 못 찾자(영어권 검색이라 한국 학회가
잡히지 않았다) 근거를 대라고 몰아붙였고 **맞는 답이 철회됐다.** 그 철회가 '양쪽의
합의' 로 기록됐다. 학회 홈페이지를 직접 열고서야 바로잡혔다.

둘 다 같은 것을 말한다. **모델이 몇이든 동의는 근거가 아니다.** 같은 오해를 나눠
갖거나, 한쪽이 다른 쪽에 끌려간다.

그래서 통과 기준을 **원 출처 확인**으로 바꿨다. 합의는 '무엇을 확인할지' 고르는
단계로만 쓰고, 단정으로 싣는 것은 공식 문서·기관 홈페이지·저장소 파일을 실제로
열어 본 것에 한한다. 확인하지 못한 것은 마지막 절에 이유와 함께 모아 적는다.

## 두 모델을 쓰는 값은 '표가 하나 더' 가 아니다

이 방의 이야기는 한국 사회복지 현장 — 학회·정부 지침·인증 제도·국내 요금제다.
영어권 검색은 이 자료를 거의 못 찾고, 저장소 파일은 검색으로 볼 수 없다. 둘을
함께 쓰는 값은 **서로 못 보는 자료를 본다**는 데 있지, 판단이 하나 더 늘어서가
아니다. 실제로 `firebase.json` 에 Functions 가 있다는 것은 파일을 읽어서, 웹와치가
주식회사라는 것은 한국어 검색으로 각각 나왔다.

## 어디에 무엇이 있나

| | 사람 보고서 | AI 보고서 |
|---|---|---|
| 파일 | `output/reports/t-XXX.md` | `output/ai-reports/t-XXX.md` |
| 규칙 원본 | `topic_reports.REPORT_RULES` | `topic_reports.AI_REPORT_RULES` |
| 읽기 | `load_reports` / `apply_reports` | `load_ai_reports` / `apply_ai_reports` |
| 얹는 곳 | `t["report"]` | `t["ai_report"]` |
| 화면 | 카드의 '보고서 읽기' | 그 아래 `.tc-ai` 블록 |

**모든 주제에 있어야 하는 글이 아니다.** 인사·가입·안부처럼 대조할 사실이 없는
대화에는 쓰지 않는다.

## 관련

- 규칙·검사: `scripts/topic_reports.py`
- 프롬프트·다시쓰기: `scripts/classify_unsorted.py`
- 테스트: `tests/test_report_rules.py` (규칙이 다시 갈라지면 여기서 걸린다)
- 자동화 전체: [AUTOMATION.md](AUTOMATION.md)
